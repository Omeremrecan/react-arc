export namespace AppContextTypes {
  export type Colors = {
    main: string;
    primary: string;
    secondary: string;
    danger: string;
    warning: string;
    info: string;
    success: string
    border:string
  };

  export type Model = {
    colors: Colors;
  };
}
